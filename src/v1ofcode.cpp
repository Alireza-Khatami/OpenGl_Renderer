    //unsigned int buffer;
    //GLLOGCALL(glGenBuffers(1, &buffer));
    //GLLOGCALL(glBindBuffer(GL_ARRAY_BUFFER , buffer));
    //GLLOGCALL(glBufferData(GL_ARRAY_BUFFER, sizeof(position), position, GL_STATIC_DRAW));
    //GLLOGCALL(glEnableVertexAttribArray(0));
    //GLLOGCALL(glVertexAttribPointer(0, 2, GL_FLOAT , GL_FALSE, 2 * sizeof(float), 0));

        //GLLOGCALL(unsigned int ibo);
    //GLLOGCALL(glGenBuffers(1, &ibo));
    //GLLOGCALL(glBindBuffer(GL_ELEMENT_ARRAY_BUFFER, ibo));
    //GLLOGCALL(glBufferData(GL_ELEMENT_ARRAY_BUFFER, sizeof(indices), indices, GL_STATIC_DRAW));